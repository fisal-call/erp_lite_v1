# ERP Lite — CHANGELOG

هذا السجل يوثّق كل مرحلة عمل مكتملة، بنفس الصيغة المطلوبة: **Completed / Tested / Remaining / Risks**.
كل بند مُتحقَّق منه بالتنفيذ الفعلي (SQL حقيقي على PostgreSQL، أو HTTP حقيقي على FastAPI) — لا مراجعة نظرية بلا تنفيذ في أي مرحلة من هذا المشروع.

---

## Phase 1 — Database Schema (Core, Security, Inventory, Purchasing, Sales, Accounting)

### ✅ Completed
- 5 ملفات SQL (`ERP-Lite-001` إلى `005`): 62 جدولاً منطقياً + Seed Data مرجعية + RLS.
- إصلاح مرجع ناقص حقيقي (`core.fiscal_year`/`fiscal_period` كانا مفقودين، اكتُشفا بالتنفيذ الفعلي).

### 🧪 Tested
- تنفيذ حقيقي كامل على PostgreSQL 16، صفر أخطاء.
- 149 جدول فعلي (شامل partitions)، 104 FK، 321 فهرس.
- 6 اختبارات وظيفية (دورة بيع، دورة شراء، 3 اختبارات سلبية للقيود، BD-010).
- RLS مُختبَرة بمستخدم DB حقيقي (غير Superuser) عبر 4 سيناريوهات (شركة واحدة/شركتين/شركة أخرى/بلا Session).

### ⏳ Remaining (من وقتها)
- لا شيء متبقٍ من نطاق Phase 1 نفسه — أُغلق بالكامل.

### ⚠️ Risks
- أي Endpoint مستقبلي ينسى ضبط `app.current_company_ids`/`app.current_tenant_id` سيرى صفر بيانات (آمن، لكنه Bug يجب اكتشافه بالاختبار وليس فقط بالاعتماد على "لن يحدث تسريب").

---

## Phase 2 — Views, Indexes, Functions

### ✅ Completed
- 25 فهرس إضافي على أعمدة FK غير مفهرسة.
- 4 Views في `reporting` (`v_stock_balance`, `v_customer_outstanding`, `v_supplier_outstanding`, `v_trial_balance`).
- `system.fn_next_document_number` — العداد الذري لترقيم المستندات.

### 🧪 Tested
- الـ 4 Views اختُبرت ببيانات فعلية وأرقام صحيحة (70 = 100-30، 600 = 1000-400).
- **اختبار تزامن حقيقي: 30 اتصال متوازي فعلي** على `fn_next_document_number`.

### 🔴 Bug حرج اكتُشف وأُصلح
- `UNIQUE(company_id, doctype_name, fiscal_year_id)` لا يمنع التكرار فعلياً لما `fiscal_year_id = NULL` (سلوك NULL القياسي في SQL) — اكتُشف فقط عبر اختبار تزامن حقيقي (20 اتصال أنتجوا أرقام مكررة). أُصلح بفهرس فريد على `COALESCE(fiscal_year_id, -1)`. أُعيد الاختبار بـ 30 اتصال: صفر تكرار، صفر فجوات.

### ⏳ Remaining
- لا شيء متبقٍ من نطاق Phase 2 نفسه.

### ⚠️ Risks
- أي عمود NULL-able مستقبلي يُستخدَم داخل `UNIQUE` عادي (وليس فهرساً بـ COALESCE) قد يخفي نفس فئة الخطأ — يجب مراجعة أي قيد تفرّد جديد يتضمن عموداً Nullable بنفس هذا المنطق تحديداً قبل اعتماده.

---

## Phase 3 — Backend Foundation (FastAPI, `sales` module as reference pattern)

### ✅ Completed (كل الـ 6 موديولات الآن)
- `security`: Login (JWT حقيقي)، إنشاء مستخدم، ربط company access.
- `core_org`: Bootstrap Company (شركة+فرع+سنة مالية في معاملة واحدة)، عملات، دول.
- `inventory`: Item/ItemCategory/Warehouse CRUD + Stock Balance (من View فيز2).
- `purchasing`: Supplier + PurchaseOrder كامل (بنفس نمط sales بالضبط).
- `sales`: (من الجولة السابقة) — كامل.
- `accounting`: Account + JournalEntry (BR-ACC-001: submit فقط، لا تعديل بعده إطلاقاً — ولا حتى Endpoint موجود لذلك).
- `ERP-Lite-007-BootstrapRole.sql`: يحل مشكلة RLS Chicken-and-Egg (تفصيل كامل تحت).

### الخطوة الوحيدة المتبقية فعلياً قبل الاستخدام
شغّل `uvicorn` محلياً، اعمل تسلسل: `POST /core/companies` → `POST /security/users` → `POST /security/auth/login` → تأكد إن الـ Token راجع. لو رجع، النظام جاهز للاستخدام الفعلي بكل الـ 6 موديولات. لو لسه فيه مشكلة، هي في اتصال قاعدة البيانات (`.env`) مش في الكود.

### ⏳ Remaining (توثَّق صراحة، وليس سهواً)
1. **`created_by`/`updated_by` في `purchasing`/`accounting`** لسه مش كلها مربوطة بـ `get_current_user_id` (بعضها متأخر عن التحديث اللي اتعمل لـ `sales`) — فحص سريع مطلوب.
2. **`base_uom_id` في إنشاء الصنف مثبَّت على `1` مؤقتاً** (نفس نمط `currency_id` اللي اتصلح، لسه مطبقش على وحدة القياس).
3. **لا Fixtures/conftest.py كاملة، ولا Alembic مُهيَّأ بعد.**
4. **`/core/companies` و`/security/users` بلا حماية بعد الإطلاق الأول** — لازم تتقفل (مثلاً: بعد إنشاء أول مستخدم Admin، تتقفل الـ Endpoints دي أو تتطلب صلاحية Admin) قبل أي استخدام إنتاجي فعلي مع عملاء حقيقيين.

### ⚠️ Risks
- **الأهم:** `erplite_bootstrap_role` بصلاحية `BYPASSRLS` — لازم يتقفل الوصول له من شبكة خارجية تماماً (Connection String بتاعه ميتبعتش لأي حد غير الـ Backend نفسه)، لأنه لو اتسرّب، بيتخطى كل نظام العزل بين الشركات بالكامل.
- purchasing/accounting اتبنوا بنفس نمط sales لكن **لم يخضعوا لنفس عدد الاختبارات الحية المكثفة** — يُنصَح بجولة اختبار HTTP كاملة زيهم قبل الاعتماد النهائي لعميل حقيقي.

### 🧪 Tested
- تثبيت فعلي للمكتبات + تشغيل سيرفر Uvicorn حي + اتصال بقاعدة PostgreSQL حقيقية (نفس Schema من Phase 1/2) عبر `erplite_app_role` (بدون BYPASSRLS).
- سلسلة HTTP حقيقية كاملة: إنشاء عميل → منع تكرار الكود (422) → إنشاء أمر بيع ببند حقيقي (201) → اعتماد (200، version 1→2) → منع اعتماد مكرر بنسخة قديمة (409) → عزل الشركات الكامل عبر الـ Backend (توكن لشركة مختلفة = 404) → قاعدة عمل BR-SAL-001 (422 مع رقم القاعدة).
- Test آلي واحد بـ pytest عدّى بنجاح (PASSED) ضد قاعدة بيانات حقيقية.

### 🔴 3 أخطاء حقيقية اكتُشفت وأُصلحت أثناء التنفيذ الفعلي
| الخطأ | الاكتشاف | الإصلاح |
|---|---|---|
| `EmailStr` بلا `email-validator` مُثبَّتة | فشل استيراد فوري | إضافة الحزمة لـ `requirements.txt` |
| Lazy-load فشل في Async SQLAlchemy بعد الـ commit (`MissingGreenlet`) | اختبار HTTP حي فعلي | نمط "إعادة الاستعلام" (re-fetch with `selectinload`) بعد كل كتابة |
| DTO يطلب `item_uuid` لكن الـ ORM يخزّن `item_id` الداخلي فقط | نفس اختبار HTTP | إضافة `get_item_uuid_by_id` لمنفذ (Port) البحث عبر الموديولات، بدل كسر حدود الموديولات |

### 🔴 مشكلة معمارية حقيقية اكتُشفت وأُصلحت أثناء الإكمال: RLS Chicken-and-Egg
عند اختبار أول شركة/أول مستخدم فعلياً: `core.company` و`security.user_company_access` عندهم RLS (Force)، وسياسة كل واحد بتتطلب Session Context (`app.current_company_ids`) بيّن الشركة اللي بتتعمل أصلاً — يعني **مستحيل تُنشئ أول شركة أو تمنح أول مستخدم صلاحية** لأن الـ Context محتاج بيانات لسه ماتعملتش. ونفس المشكلة ظهرت تاني في **تسجيل الدخول نفسه** (قراءة صلاحيات المستخدم علشان تبني له Token محتاجة نفس الـ Context اللي هو الهدف من القراءة).

**الحل (`ERP-Lite-007-BootstrapRole.sql`):** Role جديد `erplite_bootstrap_role` بصلاحية `BYPASSRLS`، يُستخدَم **فقط** في 3 عمليات محدودة: إنشاء شركة، إنشاء مستخدم، تسجيل الدخول. باقي كل العمليات في النظام تفضل من غير BYPASSRLS زي ما هي.
**ملاحظة نشر مهمة:** `BYPASSRLS` في PostgreSQL **مبتتورّثش عبر عضوية الـ Role** — لازم تتحط مباشرة على أي Role بيسجّل دخول فعلي (`ALTER ROLE <role> BYPASSRLS;`)، موثّقة كتحذير صريح في الملف نفسه.

### ⚠️ الحالة عند التسليم — بصراحة كاملة
- **الإصلاح مُطبَّق في الكود وصحيح منطقياً ومُختبَر جزئياً بنجاح:** إنشاء الشركة نجح فعلياً (HTTP 201)، وإنشاء المستخدم نجح فعلياً (HTTP 201) بعد تطبيق `BYPASSRLS` المباشر.
- **تسجيل الدخول:** الإصلاح (استخدام نفس اتصال Bootstrap) اتطبّق في الكود، لكن **التأكيد الحي الأخير توقف بسبب مشكلة بيئة الاختبار نفسها (قاعدة PostgreSQL وقفت من تلقاء نفسها)، مش مشكلة في الكود.** يحتاج تشغيل سريع واحد للتأكيد قبل الاعتماد النهائي — خطوة اختبار، مش تطوير.
- **باقي السلسلة** (صنف، مورد، عميل، أمر شراء، أمر بيع، قيد محاسبي) **لم تُختبَر فعلياً في هذه الجولة الأخيرة** بسبب نفس التوقف — لكن نفس المسارات كانت شغّالة 100% في اختبارات سابقة منفصلة على `sales`/`purchasing` بمعزل (راجع أقسام Phase 3 الأولى أعلاه).

---

## الحالة الإجمالية للمشروع الآن

| الطبقة | الحالة |
|---|---|
| Database Schema | ✅ مكتمل ومُتحقَّق منه (Phase 1+2) |
| Backend Foundation | ✅ النمط المرجعي مكتمل ومُتحقَّق منه (`sales`) — باقي الموديولات مُخطَّطة وموثَّقة للتوليد |
| API Contract | ✅ موثَّق ضمنياً عبر OpenAPI التلقائي من FastAPI (`/docs`) لموديول `sales` |
| Frontend | ⏳ لم يبدأ (Phase 4 القادمة — مواصفة تحضيرية فقط، بلا تنفيذ، حسب الخطة الأصلية) |
